import React from "react";
import Hero from "../hero/Hero";
import Cards from "../cards/Cards";

const Home = () => {
    return (
        <div>
            <Hero />
            <Cards />
        </div>
    );
};

export default Home;
